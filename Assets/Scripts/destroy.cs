using UnityEngine;
using System.Collections;

public class destroy : MonoBehaviour {
  /*  [SerializeField]
    float punishTime;

    void Update()
    {
        
        if (punishTime > 0)
            punishTime -= Time.deltaTime;
    }
    void OnTriggerEnter2D(Collider2D enemy)
    {
        if (punishTime > 0f)
        {
            if (enemy.gameObject.tag == "Low")
            {
                if (Input.GetKey(KeyCode.Z))
                {
                    Debug.Log("test");
                    Score.addScore();
                    Destroy(this.gameObject);
                }
            }
            else if (enemy.gameObject.tag == "Mid")
            {
                if (Input.GetKey(KeyCode.X))
                {
                    Score.addScore();
                    Destroy(this.gameObject);
                }
            }
            else if (enemy.gameObject.tag == "MidTwo")
            {
                if (Input.GetKey(KeyCode.C))
                {
                    Score.addScore();
                    Destroy(this.gameObject);
                }
            }
            else if (enemy.gameObject.tag == "High")
            {
                if (Input.GetKey(KeyCode.V))
                {
                    Score.addScore();
                    Destroy(this.gameObject);
                }
            }
            else
            {
                punishTime = 0.2f;
            }
        }
    }*/
}
